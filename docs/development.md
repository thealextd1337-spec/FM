# Entwicklung und Veröffentlichung

## Spielstände beim neuen Weltmodell

Für künftige Änderungen an Spielregeln, Statistiken oder Datenformaten gelten neue Berechnungen ab ihrer Einführung für neue Ereignisse. Für bereits gespeicherte Partien und Saisons keine neue rückwirkende Berechnung, Umrechnung oder zusätzliche Kompatibilitätsvariante nur für Altstände einführen. Fehlende alte Werte werden als nicht erfasst behandelt. Die Expansion auf sechs Länder darf bisherige Spielstände inkompatibel machen.

Für die Expansion auf sechs Länder müssen bisherige Spielstände nicht kompatibel bleiben. Die neue Vereinswelt verwendet ein eigenes, dauerhaft gekennzeichnetes Datenformat; alte Partien und Trainerhistorien werden nicht nachberechnet. Vorhandene Browserdaten werden beim Anlegen einer neuen Welt nicht automatisch gelöscht. Der Entwurf steht in [KI-Vereine und Trainer](ki-vereine-trainer-entwurf.md).

## Code, Prüfung und Veröffentlichung

- Quellstand: `dist/`. Die versionierten Skripte werden in der Reihenfolge von `dist/index.html` geladen; neuere Dateien überschreiben teils ältere Funktionen. Vor Änderungen die ganze relevante Aufrufkette prüfen.
- `node work/server.cjs` startet lokal auf Port 4173. `node work/build.cjs` erzeugt die Einzeldatei `outputs/index.html`. `outputs/` und temporäre Dateien bleiben außerhalb von Git.
- Passende Tests aus `work/test-*.cjs` ausführen. `node work/test-v31.cjs` simuliert zwölf Saisonen; `node work/test-v35.cjs` prüft die Transferansicht und den übernommenen Saisonablauf; `node work/test-v36.cjs` prüft die Jugendverpflichtung mit und ohne Scouting. Bei Änderungen an Finanzen, Transfers oder Saisonwechsel mindestens den Dauertest erneut ausführen.
- `node work/test-transfer-confirmations.cjs` prüft den bestätigungspflichtigen Ergebnisdialog einschließlich direkter Verpflichtung und gespeichertem Zustand. `node work/test-transfer-deadline-dialog.cjs` prüft die Gehaltsinformation und den bestätigten Transferschluss. `node work/test-tactics-replacement.cjs` und `node work/test-quick-lineup-cells.cjs` sichern die automatische Einsatzposition aus dem Raster und eindeutige Rasterfelder ab.
- `node work/test-progress-v58.cjs` prüft die Leitaktion für Sponsor, Transfers, Aufstellung, Match, Bericht, Ergebnisse und Saisonabschluss. `node work/test-progress-menu.cjs` prüft das feste, zugängliche Menü neben dieser Aktion. `dist/progress-v58.js` bestimmt nur die nächste UI-Aktion; die bestehenden Spielfunktionen bleiben für Regeln, Bestätigungen und Speichern zuständig.
- `node work/test-live-player-v59.cjs` prüft anklickbare Live-Spielerkarten und Spielfeld-Icons beider Teams, vollständige Pause/Fortsetzung der Simulation und die angeglichenen Abschlussdialoge.
- `node work/test-player-seasons.cjs` prüft die Reihenfolge aller Saisonstatistiken und der Karriereübersicht sowie die Navigation per Knopf, Scrollen und Tastatur.
- `node work/test-v41.cjs` prüft Pokalspielplan, getrennte Statistiken, Sieger-Award, einmalige Prämie und Elfmeterschützen.
- `node work/test-v42.cjs` prüft das gespeicherte Elfmeterschießen nach einem Remis und die isolierte Demo.
- `node work/test-v43.cjs` prüft Vereinsrekorde und Awardhistorie.
- `node work/test-v44.cjs` prüft Trikot- und Logowahl, Kontrast und Torwarttrikot.
- `node work/test-v45.cjs` prüft die gespeicherten Torwarttrikotpaare aller Vereine, die Auswahl im Match und die Ergänzung bei bestehenden Karrieren.
- `node work/test-v50.cjs` prüft Fouls, Freistöße, Ecken, Blocks, Abpraller, reguläre Elfmeter einschließlich der Torbeschriftung und überschneidungsfreier Anzeigen, Aggressivität, Halbzeit, Spielende und den Pokalübergang.
- `node work/test-v50-balance.cjs` simuliert 108 vollständige Matches mit drei eigenen und drei gegnerischen Aggressivitätsstufen. Der Test prüft die Foulhäufigkeit, Standardsituationen und Spieldauer.
- `node work/test-v51.cjs` prüft gekoppelte Form und Frische, Ermüdung im Match und den gespeicherten Frischewert nach Abpfiff. `node work/test-v52.cjs` prüft den stufenlosen Balken, Variante A sowie die einmalige Erholung nach Match und Neuladen. `node work/generate-status-icons.cjs` erzeugt die SVG-Symbole, die Vorschau und ihre Daten für den Einzeldatei-Build.
- `node work/test-mobile-drag-scroll.cjs` prüft die Richtung und Auslöseschwelle mobiler Touch-Gesten auf der Taktikansicht sowie die native vertikale Scrollfreigabe für Spielfeld, Startelf und Bank.
- `node work/test-v55.cjs` prüft neue Spielstände und Computerkader mit Fähigkeiten von 1–20, den sichtbaren Einwurf, Abseits und den kurzen Freistoß. Der frühere `test-v53.cjs` und sein 0–20-Prüfspielstand beschreiben den nicht mehr ladbaren Vorgängerstand.
- `node work/test-breakaway.cjs` prüft freie Läufe beider Teams, Abschlüsse, Querpass und Abseitsfilter sowie die ausbleibende Verfolgung eines Torwarts mit sicherem Ballbesitz. `node work/test-v47.cjs` prüft außerdem alle abgeschlossenen Pokalpartien in der Ergebnisübersicht.
- `node work/test-statistics-menu.cjs` prüft den eigenen Statistik-Reiter, Top-10-Listen ohne Nullwerte, Profilknöpfe für eigene und KI-Spieler ohne Profil-ID, Torwart-Zu-null-Serie sowie gespeicherte Fouls und Elfmeter beider Teams einschließlich Pokal-Elfmeterschießen. Der Match-Balancetest sichert auch den Abpfiff nach einer Standardsituation oder einem späten Tor ab.
- `node work/test-ai-season-stats.cjs` prüft die Saisonwerte parallel simulierter KI-Ligaspiele einschließlich Assists, Zu-null-Spielen, Gegentoren, Fouls und Elfmetern. Es sichert die einmalige Verbuchung und die genaue Nachtragung von Zu-null-Spielen aus älteren Spielständen ab.
- `node work/test-ai-career-v60.cjs` prüft KI-Rotation, Form, Frische, einmalige Erholung, dynamische Stärke, Liga- und Pokalverlauf über eine Saison sowie unveränderte Werte nach erneutem Laden.
- `node work/generate-world-catalog-v61.cjs` übernimmt die 48 Vereinsidentitäten und relativen Startprofile aus dem Vereinskatalog in `dist/world-catalog-v61.js`. `dist/world-foundation-v61.js` erzeugt aus einem gespeicherten Welt-Seed für jeden Verein elf feste Profis und stellt den mehrstufigen Spielstart bereit; `dist/world-start-v61.css` gestaltet diese Ansichten. Die Vereinswelt mit Nachwuchs und Managerwechsel nutzt Schema 13 und Modellversion 9. Neue Karrieren liegen in IndexedDB (`doppel6-world-v8`); ohne IndexedDB nutzt die Testumgebung `sechser.world.v8` in `localStorage`. Ältere Aufbau-Spielstände werden nicht umgerechnet. `node work/test-world-foundation-v61.cjs` prüft IDs, Clubwahl, Kader, Modellkennung, Managerzuordnung und Katalog.
- `dist/world-competition-v62.js` plant und verarbeitet die zehn Ligaspieltage in sechs Ländern, sechs nationale Pokale und den Europacup auf einem gemeinsamen Kalender. Die Ergebnisse sind aus Welt-Seed und Spiel-ID reproduzierbar. `node work/test-world-competition-v62.cjs` prüft unter anderem 259 eindeutige Partien je Saison, Terminabstände, Europacup-Heimrecht, Qualifikation, Saisonwechsel und Speichergröße nach zehn Saisons. Parallelpartien verwenden den gemeinsamen kompakten Matchlauf aus `dist/world-match-v64.js`; die Weltwirtschaft hängt an den einmaligen Match- und Saisonereignissen.
- Die Karriereansicht der neuen Welt verwendet `v46Tabs` aus `dist/main-menu-v46.js` als Menüvokabular und die vorhandene `v58Bar` aus `dist/progress-v58.js` für den nächsten Schritt. Sie bindet keine alten Spielstanddaten an diese Ansichten. Erreichbare Reiter haben eigenen Inhalt; Transfers sind mit der Marktetappe aktiv. Bei Anpassungen an v46/v58 beide Karrierepfade im Browser prüfen.
- `dist/world-coaches-v63.js` hält Trainer als getrennte Weltobjekte mit eindeutiger Vereinszuordnung, gespeichertem Vorab-Erwartungswert je Partie und Amtsgeschichte. `v62AdvanceDay` bewertet Trainer nach dem einmalig verbuchten Ergebnis; `v62NextSeason` aktualisiert freien Kandidatenpool und Laufbahnenden. `node work/test-world-coaches-v63.cjs` prüft deterministische Anlage, einmalige Bewertung, Entlassung, Interimspfad und Saisonfristen. Trainerpräferenzen bestimmen seit v64 die anfängliche KI-Taktik und ihre Reaktionen während der Partie.
- `dist/world-match-v64.js` bereitet Startelf, Bank und Taktik für beide Seiten vor und verarbeitet Parallelspiele mit einem kompakten Ereignislauf. `dist/world-physical-v65.js` bindet für eigene Partien die bisherige physikalische Engine mit Ballaktionen, Abseits, Einwürfen und Standards ein. Der Adapter überträgt die Weltkader, taktische Änderungen und bis zu zwei vorgemerkte Wechsel je Seite; bei Abpfiff verbucht er Ergebnis, Einsatzminuten, Frische und Spielerstatistiken einmalig in der Welt. Sichere Zustände ohne laufenden Ballflug werden laufend gespeichert und nach Neuladen fortgesetzt. `node work/test-world-match-v64.cjs` prüft gültige Aufstellungen, Taktikänderungen, Unterbrechungswechsel, Pause, Einsatzminuten, Reproduzierbarkeit und einmalige Ergebnisbuchung des kompakten Laufs. Die physikalische Integration zusätzlich im Browser vollständig durchspielen. Nach dem Saisonwechsel werden detaillierte Matchzustände zu Saisonwerten verdichtet, damit zehn Saisons unter der Speichergrenze bleiben.
- Das 5×7-Raster der eigenen Weltpartie liegt in `state.cells`; `dist/world-match-ui-v64.js` rendert es vor dem Spiel und in der Pause, `dist/world-grid-touch-v71.js` ergänzt Touch-Ziehen neben HTML-Drag-and-Drop. `dist/world-physical-v65.js` übernimmt die gespeicherten Rasterpositionen in die physische Partie. `node work/test-world-grid-v71.cjs` prüft freie Felder, Tausch, Zonenregeln und Feldübergabe bei Wechseln; im Browser beide Phasen samt Drag-and-Drop und Neuladen prüfen.
- `dist/world-economy-v66.js` führt für alle 48 Vereine Kontostand, Buchungen, drei Sponsorangebote, befristete Profiverträge und den gemeinsamen Markt. `dist/world-market-ui-v66.js` bindet Sponsorwahl, Gebote, Verträge und Finanzen in die bestehenden Karrierereiter und die Fortschrittsleiste ein. Die fünf Transfertage liegen vor dem ersten Match jeder Saison; danach sind nur ablösefreie Zugänge möglich. Wechsel übertragen Spieler, Vertrag und Ablöse gemeinsam, Gehälter und Prämien werden beim Saisonabschluss einmalig gebucht. `node work/test-world-economy-v66.cjs` prüft Transferentscheidungen, Gegenangebote, Mindestkader, Neuladen, Mehrjahresfinanzen und Speichergröße; mit `DOPPEL_SEASONS=10` laufen zehn Saisons. Bei Änderungen an Markt oder Saisonabschluss auch diesen Dauertest und einen Browserdurchgang von Sponsorwahl bis Matchplan ausführen.
- `dist/world-youth-manager-v67.js` erzeugt und befristet vereinseigene Nachwuchspools, bucht jährliche Jugendbudgets, übernimmt Kandidaten gegen Ausbildungsentschädigung und entwickelt Profis ausschließlich durch neue Pflichtspielminuten. Zum Saisonabschluss werden Managerleistung und höchstens drei Angebote aus vorläufig besetzten Ligavereinen gespeichert; Annahme oder Ablehnung und Budgetwahl gehen dem nächsten Saisonstart voraus. Altersbedingte Trainerabschiede schaffen gelegentlich echte Vakanzen. `dist/world-youth-ui-v67.js` bindet Entscheidungen und Kandidaten an Übersicht, Kader und Verein. Ein Ladebalken begleitet Laden, Aufbau und längere Fortschritte. `node work/test-world-youth-manager-v67.cjs` prüft diese Regeln, Vereinswechsel, Neuladen und mit `DOPPEL_SEASONS=10` die Spielstandgröße. `node work/test-world-storage-v67.cjs` prüft die asynchrone Speicherung. Zehn Saisons ergeben derzeit rund 5,6 MB; deshalb speichert der Browser neue Vereinswelten in IndexedDB.
- Die Startseite verwaltet höchstens fünf neue Vereinswelten mit Öffnen, JSON-Export und bestätigtem Löschen. Der vorhandene Importknopf erkennt das gekennzeichnete Weltformat mit Schema 13 und Modellversion 9, prüft es vor dem Speichern und vergibt bei einer bereits vorhandenen Spielstand-ID eine neue ID. Export wartet auf laufende IndexedDB-Schreibvorgänge. Bereits gespeicherte Karrieren oberhalb der Fünfergrenze bleiben erhalten und spielbar, bis sie freiwillig gelöscht werden; neue Karrieren und Importe sind dann gesperrt. `node work/test-world-save-management-v70.cjs` prüft diese Grenzen und den Export/Import, `node work/test-world-storage-v67.cjs` auch den Export nach einem verzögerten Schreibabschluss. Die bisherigen Sechserliga-Spielstände nutzen weiterhin ihren eigenen Import- und Exportpfad.
- `dist/world-views-v68.js` und `dist/world-views-v68.css` verknüpfen Vereins-, Trainer- und Spielerprofile innerhalb der sechs Reiter mit Rücknavigation, Weltüberblick, Siegerarchiv und klar benannten Wechseln im Spielverlauf. Längere Fortschritte bis zum eigenen Match geben der Oberfläche zwischen Weltspieltagen Zeit zum Zeichnen; das vorhandene Ladeelement meldet die Zahl verarbeiteter Spieltage. Die Tabellen bleiben auf schmalen Bildschirmen horizontal bedienbar; offene Wettbewerbe nutzen dort den normalen Seitenscroll. `node work/test-world-views-v68.cjs` prüft Profilzugriff, Nachrichtenfilter, Ergebnislinks und Wechselanzeige. `node work/audit-world-balance-v68.cjs` simuliert standardmäßig sechs feste Seeds über je zehn Saisons. Es erfasst Rangabweichungen und Varianz je Verein, Titelserien, Pokalstärke nach Saison, Transfers, Jugendminuten, Gehaltsanteile, Trainerereignisse, KI-Finanzen und Spielstandgröße. Nach Markt, während der Saison und am Saisonende werden serialisierte Spielstände sowie eindeutige Aufstellungen, Ereignisse und Verträge geprüft. Der Audit akzeptiert im Mehrjahresmedian bei Pokalvereinen einen ungefähren Qualitätsabstand mit sportlich begründeten Ausnahmen; seine Qualitätsgröße ist der mittlere Fähigkeitswert des Profikaders. Die Startkadererzeugung kalibriert nur neu erzeugte Pokalkader; gespeicherte Kader werden nicht verändert.
- Die Abnahme und Messergebnisse stehen in [abnahme-welt-v69.md](abnahme-welt-v69.md). Ligavereine gewichten bei KI-Geboten stärkere Kandidaten und behalten eine Kostenreserve. Der regionale Grundbetrag der Pokalvereine beträgt 100 Credits; ihre KI-Jugendbudgets sind kleiner und nach Jugendkultur gestuft. Die Qualität neu erzeugter Jugendspieler reagiert stärker auf das bezahlte Budget. Vor KI-Vertragsverlängerungen prüft `v66SecureNextIncome` die festen Einnahmen des Folgejahres gegen bereits zugesagte Gehälter und eine Reserve. `work/test-world-economy-v66.cjs` enthält dafür einen gezielten Insolvenztest. Neue Regeln wirken nur auf künftige Entscheidungen und neue Spieler; alte Ergebnisse und Spielerwerte werden nicht umgerechnet.
- `node work/test-v56.cjs` prüft die körperliche Reichweite von Zweikämpfen, sichtbare Grätschen mit Ball- oder Körperkontakt einschließlich sauberem Ballgewinn und Fouls von hinten, die Ballaktion nach einem gescheiterten Zweikampf, Erholung nach Balleroberung, Fouls ohne Karten oder Platzverweise, die volle Spielerzahl, Abstand bei Luftduellen und vollständige Matches. `node work/test-penalty-frequency.cjs` simuliert 48 Matches mit der aktuellen Engine und prüft ballseitige Grätschen von vorn und der Seite, seltene Grätschen von hinten sowie die Häufigkeit regulärer Elfmeter. `node work/test-duel-streaks.cjs` prüft eine reproduzierbare Matchsequenz auf Zweikampf-Pingpong; mit `DOPPEL_DUEL_SWEEP=1` werden 24 Seeds simuliert.
- `node work/test-v57.cjs` prüft die Positionierung beider Teams bei Freistoß und Abseits, Tororientierung bei breiten Freistößen, Rollenreihenfolge beider Mannschaften, ein mögliches Tor aus direktem Freistoß, die 1,5 Sekunden eingefrorene Abseitsszene mit pausierter Spieluhr sowie Ausblendung und anschließende halbe Sekunde Spielpause.
- GitHub-Repository: `thealextd1337-spec/FM` (öffentlich). Ein Push auf `main` startet den Deployment-Workflow für `https://fussball.cakamper.at/` auf World4You; der Workflow kann auch manuell gestartet werden. Vor einer Veröffentlichung Build und Live-Version gesondert prüfen. Bestehende Branch-Historie vor Push oder Merge prüfen und nicht mit Force Push überschreiben.

## Stärkeskala und neue Matchregeln

`dist/strength-v55.js` erzeugt Spieler und Computerkader direkt mit ganzzahligen Fähigkeiten von 1 bis 20. `ability()` liefert effektive Matchwerte auf derselben Skala; Form, Frische, Alter, Müdigkeit und fremde Einsatzposition werden dort berücksichtigt. Scouting, Transferqualität, Gehälter, Saisonereignisse und Elfmeterschießen rechnen direkt mit der 20er-Skala. Frischeprozente, Geldbeträge, Wahrscheinlichkeiten und Spielnoten behalten eigene Einheiten.

Die Fähigkeitszahlen bleiben intern und im Spielstand erhalten. Oberflächen einschließlich Kader, Profil, Transfer, Jugend und zugänglicher Beschriftungen dürfen nur Farbstufen oder qualitative Angaben ausgeben; auch erworbene Fähigkeitspunkte werden nicht als Zahl gezeigt.

`dist/pitch-v55.js` bündelt Abwehrlinie, Einsatzposition, hohe Bälle, Flanken, Luftduelle, Einwurf und Abseits. Alte Spielstände werden auf ausdrücklichen Produktentscheid hin nicht migriert: gelesen werden nur `sechser.saves.v5` und Exporte mit Schema 5. Die Computerkader entstehen in jeder neuen Karriere erneut. `dist/strength-v53.js` ist ein nicht mehr geladenes Vorgängerskript mit alten Kompatibilitätsumrechnungen; neue Aufrufstellen dürfen es nicht verwenden. Der Build und der lokale Server müssen neue Skripte in ihrer Dateiliste führen.

`dist/ai-career-v60.js` wählt die KI-Elf positionsgerecht und aktualisiert nach jeder neuen Partie ausschließlich die gespeicherten Spieler im `world`-Kader. Vor dem nächsten Einsatz löst `pendingPlayedRecovery` die einmalige Erholung aus; `lastLineup` hält die tatsächlich zuletzt genutzte Elf fest. Liga und Pokal benutzen dieselbe Auswahl, auch für parallel simulierte Spiele. Die bestehenden Ergebnisfunktionen berücksichtigen eine begrenzte Anpassung der KI-Teamstärke. Für eine spätere KI-Jugendentwicklung ist `aiCareerFinish()` der gemeinsame Abschluss nach einem Einsatz; Fähigkeitswachstum ist derzeit bewusst nicht eingeschaltet. Neue Berechnungen gelten nur für künftige Partien bestehender Spielstände.

`dist/pitch-v56.js` ersetzt die alte abstrakte Zweikampfentscheidung durch Kontakt- und Richtungsprüfung in Spielfeldpixeln. Eine Grätsche von vorn oder der Seite beginnt nur mit ballseitiger Anlaufbahn; von hinten ist die Ansatzwahrscheinlichkeit geringer und sie zielt weiter vor den laufenden Ballführer. Berührt sie den Körper, ist es ein Foul; erreicht sie vorher den Ball, kann sie legal sein. Grätschen laufen über mehrere Simulationsschritte und unterbrechen die reguläre Spielerbewegung bis zur kurzen Erholung. Nach verlorenen und verfehlten Zweikämpfen ist ein sofortiger erneuter Eingriff kurz gesperrt; der Ballführer führt nach einem verfehlten stehenden Zweikampf seine reguläre Aktion aus. Die vorhandenen Foul- und Standardsituationsregeln bleiben maßgeblich; es werden keine Karten oder Platzverweise erzeugt.

`dist/pitch-v57.js` bewegt die Spieler während der Freistoßanzeige und nach der eingefrorenen Abseitsszene zu Wiederanstoßpositionen anhand der Einsatzposition und der ursprünglichen Seitenausrichtung. Beim Abseits bleibt die Momentaufnahme einschließlich Linie, markiertem Spieler und Linienrichter zunächst für `v50OffsideFreezeSeconds` unverändert; ein mittiger Banner wird nicht gezeigt. Erst danach verschwindet die Markierung und die Bewegung beginnt. Breite Freistöße richten sich zum Tor. `dist/set-pieces-v50.js` hält Freistöße nach dem Ausblenden für weitere 0,5 reale Sekunden an; Ecken, Elfmeter und Anstöße behalten ihren bisherigen Ablauf. `dist/keeper-logo-v45.js` speichert je Verein ein festes Torwarttrikotpaar und wählt pro Match die kontrastreichere zulässige Kombination.

Bei Änderungen an der Spiellogik `node work/test-v55.cjs`, `node work/build.cjs` und einen vollständigen Browser-Match prüfen. Besonders prüfen: Ball außerhalb der Seitenlinie während des Einwurfs, verlangsamte Uhr bei weiterlaufender Bewegung, Abseitslinie und Wiederanstoß, Kurzpass nach Abseits, tatsächlicher Landepunkt hoher Bälle und keine doppelten Schüsse oder Vorlagen.
